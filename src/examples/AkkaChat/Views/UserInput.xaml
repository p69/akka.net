<UserControl
    x:Class="AkkaChat.Views.UserInput"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AkkaChat.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="400">

    <Grid Background="Aquamarine">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{x:Bind Vm.UserName, Mode=OneWay}" Margin="5,0,5,0" VerticalAlignment="Center"/>
        <TextBox Grid.Column="1" Height="20" Text="{x:Bind Vm.Text, Mode=TwoWay}"/>
        <Button Content="say" Grid.Column="2" Margin="5,0,5,0" Click="{x:Bind Vm.Say}"/>
    </Grid>
</UserControl>
